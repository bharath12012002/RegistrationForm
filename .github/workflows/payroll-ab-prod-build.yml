name: payroll Build and Push

on:
  push:
    branches:
      - hrm/develop
    paths: 
      - payroll/**


jobs:
  container_build:
    uses: ./.github/workflows/react-ab-prod-build.yml
    secrets: inherit
    with:
      react-folder: 'payroll'
      mfp-folder: 'payroll'
