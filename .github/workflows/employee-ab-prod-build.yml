name: Employee Build and Push

on:
  push:
    branches:
      - hrm/develop
    paths: 
      - Employee/**


jobs:
  container_build:
    uses: ./.github/workflows/react-ab-prod-build.yml
    secrets: inherit
    with:
      react-folder: 'Employee'
      mfp-folder: 'employee'
