name: payroll Build and Push

on:
  push:
    branches:
      - develop
    paths: 
      - payroll/**


jobs:
  container_build:
    uses: ./.github/workflows/react-dev-build.yml
    secrets: inherit
    with:
      react-folder: 'payroll'
      mfp-folder: 'payroll'
