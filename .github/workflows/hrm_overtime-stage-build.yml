name: Overtime Build and Push
# Devlop
on:
  push:
    branches:
      - hrm/stages
    paths: 
      - hrm_config_ot_permission/**


jobs:
  container_build:
    uses: ./.github/workflows/react-stage-build.yml
    secrets: inherit
    with:
      react-folder: 'hrm_config_ot_permission'
      mfp-folder: 'hrm_config_ot_permission'
