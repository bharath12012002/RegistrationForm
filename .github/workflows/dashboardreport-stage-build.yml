name: Dashboardreport Build and Push

on:
  push:
    branches:
      - hrm/stages
    paths:
      - hrdash/**

jobs:
  container_build:
    uses: ./.github/workflows/react-stage-build.yml
    secrets: inherit
    with:
      react-folder: "hrdash"
      mfp-folder: "dashboardreport"
