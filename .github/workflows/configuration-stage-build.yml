name: Configuration Build and Push

on:
  push:
    branches:
      - hrm/stages
    paths:
      - Configuration/**

jobs:
  container_build:
    uses: ./.github/workflows/react-stage-build.yml
    secrets: inherit
    with:
      react-folder: "Configuration"
      mfp-folder: "configuration"
