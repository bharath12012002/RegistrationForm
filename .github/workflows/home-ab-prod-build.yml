name: Home Build and Push

on:
  push:
    branches:
      - hrm/develop
    paths: 
      - Home/**


jobs:
  container_build:
    uses: ./.github/workflows/react-ab-prod-build.yml
    secrets: inherit
    with:
      react-folder: 'Home'
      mfp-folder: 'home_hrm'
