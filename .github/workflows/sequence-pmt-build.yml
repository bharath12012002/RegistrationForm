name: Sequence Build and Push

on:
  push:
    branches:
      - hrm/pmt-build
    paths: 
      - sequence/**


jobs:
  container_build:
    uses: ./.github/workflows/react-pmt-build.yml
    secrets: inherit
    with:
      react-folder: 'sequence'
      mfp-folder: 'sequence'
