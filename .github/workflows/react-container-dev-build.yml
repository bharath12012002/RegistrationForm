name: React Js Pentagon HRM Dev Build

on:
  workflow_call:
    inputs:
      react-folder:
        required: true
        type: string
      mfp-folder:
        required: true
        type: string
env:
  IPSS_FE_PRODUCTION_DOMAIN: "https://ipss.asset.techgenzi.com/mfp"

jobs:
  react_build:
    name: React Js Folder Build
    runs-on: ubuntu-latest
    steps:
      - name: git-checkout
        uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - uses: actions/cache@v2
        with:
          path: ${{ format('{0}/node_modules', inputs.react-folder) }}
          key: ${{ runner.os }}-modules-${{ hashFiles('**/package-lock.json') }}
      - name: Yarn Install
        working-directory: ${{ inputs.react-folder }}
        run: |
          npm i --legacy-peer-deps
      - name: Dev Build
        working-directory: ${{ inputs.react-folder }}
        run: |
          npm run build
          cp -r dist build
          cp -r public build/public
      - name: Deploy to S3
        uses: jakejarvis/s3-sync-action@master
        with:
          args: ""
        env:
          AWS_S3_BUCKET: ${{ secrets.TG_ASSET_AWS_S3 }}
          AWS_ACCESS_KEY_ID: ${{ secrets.TG_ASSET_AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TG_ASSET_AWS_SECRET_KEY }}
          AWS_REGION: ${{ secrets.TG_ASSET_AWS_REGION }}
          SOURCE_DIR: ${{ format('{0}/build', inputs.react-folder) }}
