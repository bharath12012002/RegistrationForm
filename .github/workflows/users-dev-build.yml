name: Users Build and Push

on:
  push:
    branches:
      - develop
    paths:
      - users/**

jobs:
  container_build:
    uses: ./.github/workflows/react-dev-build.yml
    secrets: inherit
    with:
      react-folder: "users"
      mfp-folder: "users"
