name: Incentive Build and Push

on:
  push:
    branches:
      - develop
    paths: 
      - Incentive/**


jobs:
  container_build:
    uses: ./.github/workflows/react-dev-build.yml
    secrets: inherit
    with:
      react-folder: 'Incentive'
      mfp-folder: 'incentive'
