name: Sequence Build and Push

on:
  push:
    branches:
      - hrm/develop
    paths: 
      - sequence/**


jobs:
  container_build:
    uses: ./.github/workflows/react-ab-prod-build.yml
    secrets: inherit
    with:
      react-folder: 'sequence'
      mfp-folder: 'sequence'
