name: Attendance Build and Push

on:
  push:
    branches:
      - hrm/pmt-build
    paths: 
      - Attendance/**


jobs:
  container_build:
    uses: ./.github/workflows/react-pmt-build.yml
    secrets: inherit
    with:
      react-folder: 'Attendance'
      mfp-folder: 'attendance'
