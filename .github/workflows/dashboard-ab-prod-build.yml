name: Dashboard Build and Push

on:
  push:
    branches:
      - hrm/develop
    paths: 
      - Dashboard/**


jobs:
  container_build:
    uses: ./.github/workflows/react-ab-prod-build.yml
    secrets: inherit
    with:
      react-folder: 'Dashboard'
      mfp-folder: 'dashboard'
