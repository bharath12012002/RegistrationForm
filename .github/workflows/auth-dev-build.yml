name: Auth Build and Push

on:
  push:
    branches:
      - develop
    paths: 
      - Auth/**


jobs:
  container_build:
    uses: ./.github/workflows/react-dev-build.yml
    secrets: inherit
    with:
      react-folder: 'Auth'
      mfp-folder: 'auth'
