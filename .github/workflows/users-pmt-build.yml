name: Users Build and Push

on:
  push:
    branches:
      - hrm/pmt-build
    paths:
      - users/**

jobs:
  container_build:
    uses: ./.github/workflows/react-pmt-build.yml
    secrets: inherit
    with:
      react-folder: "users"
      mfp-folder: "users"
